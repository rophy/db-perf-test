apiVersion: v2
name: yb-benchmark
description: YugabyteDB benchmark suite with sysbench, HammerDB, and Prometheus
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: prom-replay
    version: "0.2.1"
    repository: "oci://ghcr.io/null-ptr-exception/prom-replay/charts"
    condition: prom-replay.enabled

